[phases.setup]
nixPkgs = ["python3", "ffmpeg", "nodejs_20"]

[phases.install]
cmds = [
  "python -m venv /opt/venv && /opt/venv/bin/pip install --upgrade pip && /opt/venv/bin/pip install -r requirements.txt"
]

[phases.start]
cmds = [
  "/opt/venv/bin/python bot.py"
]